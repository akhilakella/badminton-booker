services:
  - type: web
    name: badminton-booker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class eventlet -w 1 app:app --timeout 120
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
